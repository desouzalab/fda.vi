% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_elbo.R
\name{elbo_functions}
\alias{elbo_functions}
\title{ELBO Component Functions}
\arguments{
\item{y}{List of observed curve vectors.}

\item{ni}{Integer vector of per-curve sample sizes.}

\item{B}{List of basis matrices.}

\item{i}{Integer. Curve index (per-curve terms only).}

\item{m}{Integer. Number of curves.}

\item{K}{Integer. Number of basis functions.}

\item{iter}{Integer. Current iteration index.}

\item{delta_1, delta_2}{Prior hyperparameters for \eqn{\sigma^2}.}

\item{lambda_1, lambda_2}{Prior hyperparameters for \eqn{\tau^2}.}

\item{delta1_q, delta2_values}{Shape and scale trajectory of \eqn{q(\sigma^2)}.}

\item{lambda1_q, lambda2_values}{Shape and scale trajectory of \eqn{q(\tau^2)}.}

\item{mu_beta_values}{Matrix of posterior beta mean trajectories.}

\item{Sigma_beta}{Array of posterior beta covariance matrices.}

\item{a1_values, a2_values}{Shape parameter trajectories for \eqn{q(\theta_{ki})}.}

\item{prob_values}{Matrix of inclusion probability trajectories.}

\item{mu_ki}{Scalar prior hyperparameter for \eqn{\theta_{ki}}.}

\item{psi}{Current correlation matrix \eqn{\Psi(w)}.}

\item{w}{Positive scalar. Decay parameter (\code{elbo_omega}, \code{dev_elbo} only).}

\item{Xt}{Numeric vector of evaluation points (\code{elbo_omega}, \code{dev_elbo} only).}

\item{logdet_psi}{Pre-computed \eqn{\log|\Psi|} or \code{NULL}
(\code{expectedLogLikelihood} only).}
}
\value{
A numeric scalar.
}
\description{
Internal functions computing the six additive terms of the Evidence Lower
Bound (ELBO), which serves as both the convergence criterion and the M-step
objective in the VEM algorithm (da Cruz et al., 2024, eq. 36--42).

The per-curve terms (\code{expectedLogLikelihood}, \code{elboInclusionTerm},
\code{elboBetaTerm}, \code{elboThetaTerm}) are summed over \eqn{i=1,\ldots,m}.
The global terms (\code{elboSigmaPriorTerm}, \code{elboTauTerm}) contribute once.

\code{elbo_corr} assembles all six terms into the total ELBO at fixed \eqn{w}.

\code{elbo_omega} wraps \code{elbo_corr} as a function of \eqn{w} alone,
passed to \code{optim()} as the M-step objective.

\code{dev_elbo} provides the analytic gradient \eqn{\partial\text{ELBO}/\partial w},
passed to \code{optim()} to enable L-BFGS-B optimisation.
}
\keyword{internal}
