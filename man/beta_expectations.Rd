% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_beta.R
\name{beta_expectations}
\alias{beta_expectations}
\title{Expected Residual and Coefficient Squared Moments}
\arguments{
\item{B}{List of \eqn{n_i \times K} basis matrices.}

\item{i}{Integer. Curve index.}

\item{y}{List of observed curve vectors.}

\item{mu}{Matrix (\eqn{\text{maxIter} \times mK}) of posterior beta means.}

\item{Sigma}{Array (\eqn{K \times K \times m}) of posterior beta covariances.}

\item{prob}{Matrix (\eqn{\text{maxIter} \times mK}) of inclusion probabilities.}

\item{iter}{Integer. Current iteration index.}

\item{psi}{Correlation matrix \eqn{\Psi} from \code{computePsiMatrix}.}

\item{z}{Integer (0 or 1). Candidate inclusion value (\code{expectedBetaSq} only).}

\item{k}{Integer. Basis function index (\code{expectedBetaSq} only).}

\item{K}{Integer. Total number of basis functions (\code{expectedBetaSq} only).}
}
\value{
A numeric scalar.
}
\description{
Three expectations under \eqn{q(\beta_i)} and \eqn{q(Z_i)} appearing in the
updates for \eqn{q(\sigma^2)}, \eqn{q(\tau^2)}, and \eqn{q(Z_{ki})}.

\code{expectedResidualSq} computes the expected weighted residual sum of
squares \eqn{E[(y_i - B_i\xi_i)^\top \Psi^{-1}(y_i - B_i\xi_i)]} via a
quadratic-form plus trace decomposition. Uses \code{prob[iter-1,]} because
\eqn{q(Z)} has not yet been updated at the point this is called (Step 2).

\code{expectedSumBetaSq} computes
\eqn{\sum_k(\text{Var}_{\beta_{ki}} + \mu_{\beta_{ki}}^2)}, appearing in
the \eqn{q(\sigma^2)} and \eqn{q(\tau^2)} updates.

\code{expectedBetaSq} computes the same residual quadratic form as
\code{expectedResidualSq} but with the \eqn{k}-th inclusion indicator fixed
at candidate value \eqn{z \in \{0,1\}}, used in the \eqn{q(Z_{ki})} update.
}
\keyword{internal}
